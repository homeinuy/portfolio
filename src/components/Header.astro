---
const currentPath = Astro.url.pathname;

const links = [
  { href: '/', label: 'Inicio' },
  { href: '/projects/', label: 'Servicios' },
  { href: '/desarrollos/', label: 'Proyectos' },
  { href: '/mapa/', label: 'Mapa' },
  { href: '/blog/', label: 'Blog' },
  { href: '/contact/', label: 'Contacto' },
];
---

<header class="border-b border-gray-100">
  <nav class="max-w-5xl mx-auto px-6 py-4 flex items-center justify-between">
    <a href="/" class="hover:opacity-80 transition">
      <img src="/images/homein-dark.png" alt="Homein" style="height: 32px; width: auto;" />
    </a>
    <ul class="flex gap-6 text-sm font-medium">
      {links.map((link) => (
        <li>
          <a
            href={link.href}
            class:list={[
              'transition hover:text-gray-900',
              currentPath === link.href || currentPath.startsWith(link.href) && link.href !== '/'
                ? 'text-gray-900'
                : 'text-gray-500',
            ]}
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>
  </nav>
</header>
